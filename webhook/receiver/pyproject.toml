[project]
name = "memdog-webhook-receiver"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "functions-framework==3.*",
    "google-cloud-pubsub==2.*",
    "flask>=2.0",
    "requests>=2.31.0",
]
