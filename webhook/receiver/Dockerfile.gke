FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    flask>=2.0 \
    nats-py>=2.0 \
    gunicorn>=21 \
    requests>=2.31.0

COPY gke_app.py ./

EXPOSE 8080

CMD ["gunicorn", "--bind", "0.0.0.0:8080", "--workers", "2", "--timeout", "120", "gke_app:app"]
