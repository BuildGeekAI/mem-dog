apiVersion: v1
kind: Secret
metadata:
  name: webhook-gateway-secrets
  namespace: webhook-gateway
  labels:
    app.kubernetes.io/part-of: mem-dog
    app.kubernetes.io/component: lean-local
type: Opaque
stringData:
  GEMINI_API_KEY: ""
  # Must match api-auth-secret API_KEY — lean core forwards envelopes to API ingest.
  WEBHOOK_API_KEY: "dev-local-key"
  MEM_DOG_API_KEY: "dev-local-key"
  WGW_API_KEY: ""
