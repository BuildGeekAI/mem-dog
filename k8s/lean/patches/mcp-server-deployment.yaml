apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-server
  namespace: mem-dog
spec:
  template:
    spec:
      containers:
        - name: mcp-server
          image: mcp-server:local
          imagePullPolicy: IfNotPresent
