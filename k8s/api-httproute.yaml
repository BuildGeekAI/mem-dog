# HTTPRoute that exposes the memdog API through the open-jaws Gateway.
# External path: /gke-api/api/v1/*  →  rewritten to /api/v1/* on the API service.
#
# Requires the ReferenceGrant in api-referencegrant.yaml to be applied first
# (cross-namespace backend reference from webhook-gateway → memdog).
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: memdog-api-routes
  namespace: webhook-gateway
  labels:
    app.kubernetes.io/part-of: memdog
    app.kubernetes.io/component: api
spec:
  parentRefs:
    - name: open-jaws
      namespace: webhook-gateway
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /gke-api/
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /
      backendRefs:
        - name: api
          namespace: memdog
          port: 8080
