apiVersion: v1
kind: Service
metadata:
  name: ollama
  namespace: webhook-pipeline
  labels:
    app: ollama
    app.kubernetes.io/part-of: mem-dog-webhook-pipeline
spec:
  type: ClusterIP
  selector:
    app: ollama
  ports:
    - port: 11434
      targetPort: 11434
      protocol: TCP
      name: http
