apiVersion: v1
kind: Service
metadata:
  name: ollama-chat
  namespace: webhook-pipeline
  labels:
    app: ollama-chat
    app.kubernetes.io/part-of: memdog-webhook-pipeline
spec:
  type: ClusterIP
  selector:
    app: ollama-chat
  ports:
    - port: 11434
      targetPort: 11434
      protocol: TCP
      name: http
