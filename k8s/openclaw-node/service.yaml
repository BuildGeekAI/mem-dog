apiVersion: v1
kind: Service
metadata:
  name: openclaw-node
  namespace: webhook-gateway
  labels:
    app: openclaw-node
    app.kubernetes.io/part-of: memdog
    app.kubernetes.io/component: openclaw-node
spec:
  type: ClusterIP
  selector:
    app: openclaw-node
  ports:
    - port: 18789
      targetPort: http
      protocol: TCP
      name: http
